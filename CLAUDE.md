@AGENTS.md

# Claude Code bridge

Treat `AGENTS.md` as the project source of truth. Read the relevant ADR and feature document before editing a trust boundary, and report the commands actually executed.
